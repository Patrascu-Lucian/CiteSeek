/**
 * External destinations, in one place.
 *
 * The repository is the **interim** contact route — the privacy page has to name
 * somewhere an erasure request can go. It is not the permanent answer: a repo
 * that goes private takes the policy's contact route down with it. Replaced by a
 * `/contact` page once a domain exists; see `docs/backlog.md`.
 */
export const REPOSITORY_URL = "https://github.com/Patrascu-Lucian/CiteSeek";
