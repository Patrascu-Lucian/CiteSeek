# Generated output and lockfiles — not ours to format.
pnpm-lock.yaml
# Written by `pnpm eval:retrieval`. Padding a table to its widest cell means one
# number changing width re-pads the column — noise in the diff between two runs,
# which is the only thing this file is read for.
eval/report.md
# Same, from `pnpm eval:local-answers` — and it quotes model output verbatim.
eval/local-answers*.md
eval/local-markers.md
.next/
out/
build/
coverage/
playwright-report/
test-results/
blob-report/
next-env.d.ts

# Note: components/ui/ is deliberately NOT ignored. shadcn/ui copies source into
# the repo for you to own and edit — it is not a dependency — so it is held to the
# same formatting standard as everything else.
